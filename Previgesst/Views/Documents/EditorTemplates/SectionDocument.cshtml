@(Html.Kendo().DropDownList()
            .Name("SectionId") // The name of the widget should be the same as the name of the property.
            .DataValueField("SectionId") // The value of the dropdown is taken from the EmployeeID property.
            .DataTextField("Nom") // The text of the items is taken from the EmployeeName property.
            .BindTo((System.Collections.IEnumerable)ViewData["LesSections"]) // A list of all employees which is populated in the controller.
)
